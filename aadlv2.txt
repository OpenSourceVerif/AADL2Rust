rma					√
rms					√
robotv1				√
robotv2				√
toy					√
pingpong			√ 
cpp                 √ 线程中添加优先级，cpp改成c；hello.cc改成hello.c，新增hello.h（参照rma）
lua					√ 删掉lua-ping.aadl（加上的话报错包名冲突，但实际没冲突），新增hello.h，把两个c文件合并成一个（两个会报错不知道为什么）
priority_test		√ 一个thread有多个impl -> 一个thread只有一个impl，新增ptest.h，把ping中的.c、.h文件复制过来（需要的）
code_coverage       ? 语法错误（use super::{user_emit_boolean, ()}; 中包含无效的 ()等）
				      类型名不存在（emitterThread/boolean_receiverThread 等类型未定义）
				      变量名引用错误（引用 Emitter_Thread 而非实际变量名 emitter_thread）
				      类型名命名规范错误（Rust 类型名需大写开头（如 EmitterThread），但生成了小写开头的emitterThread）
rpc					√ 拆分子程序（每个参数生成单独的函数调用），绑定端口，修改c文件（移除原来的ocarina头文件和函数调用）
file-store			√ 子组件名称改为小写，Do_Ping_Spg添加输出参数，修改c文件（移除原来的ocarina头文件和函数调用）
flight-mgmt			√ 拆分子程序，调整子程序和线程的绑定，子组件名称改为小写，修改c文件（移除原来的ocarina头文件和函数调用）
latency				√ 拆分子程序，一个子程序只有一个参数，删除多余process和system（只保留本地）
monitor				√ 调整c代码
packet-store		√ 类似file-store
producer-consumer	√ 只保留system的本地实现，修改c代码
some-types			?
some-types-stdint	?
sunseeker			包可见性（把所有包放在一个aadl文件中可以正常运行，但是放在两个aadl文件会报错）
torture_gqueue		不知道为什么没有输出结果
car 				?